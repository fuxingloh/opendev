// Popular vendor — re-exported from `@openxyz-provider/telegram/channel` so
// templates can `import { TelegramChannel } from "openxyz/channels/telegram"`
// without an extra npm install. Less-popular vendors ship as standalone
// packages (`@openxyz-provider/<vendor>`) that users add explicitly and
// import from subpaths.
export * from "@openxyz-provider/telegram/channel";
