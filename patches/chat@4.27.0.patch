diff --git a/dist/index.d.ts b/dist/index.d.ts
index 32e09eaa28f0c70659f4d9cddf945367c5d91438..e9cb01b86cd318cfb337610be3aac3a989f0bd97 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -492,6 +492,12 @@ interface ChatConfig<TAdapters extends Record<string, Adapter> = Record<string,
      * - `'queue'` — queue the message; when the current handler finishes,
      *   process only the latest queued message with `context.skipped` containing
      *   all intermediate messages
+     * - `'queue-debounce'` — like `'queue'`, but the FIRST message in an idle
+     *   thread also waits `debounceMs` so a burst that arrives while the lock is
+     *   free collapses into a single handler invocation. The handler receives
+     *   the latest message with `context.skipped` containing the rest of the
+     *   burst. Subsequent bursts arriving while the handler is running collapse
+     *   via the same `queue` drain mechanism.
      * - `'debounce'` — all messages start/reset a debounce timer; only the
      *   final message in a burst is processed
      * - `'concurrent'` — no locking; all messages processed in parallel
@@ -970,7 +976,7 @@ interface LockScopeContext {
     threadId: string;
 }
 /** Concurrency strategy for overlapping messages on the same thread. */
-type ConcurrencyStrategy = "drop" | "queue" | "debounce" | "concurrent";
+type ConcurrencyStrategy = "drop" | "queue" | "queue-debounce" | "debounce" | "concurrent";
 /** Fine-grained concurrency configuration. */
 interface ConcurrencyConfig {
     /** Debounce window in milliseconds (debounce strategy). Default: 1500. */
@@ -2999,6 +3005,15 @@ declare class Chat<TAdapters extends Record<string, Adapter> = Record<string, Ad
     private handleConcurrent;
     private acquireConcurrentSlot;
     private releaseConcurrentSlot;
+    /**
+     * Run `dispatchToHandlers` with a heartbeat that extends `lock` every
+     * `LOCK_HEARTBEAT_MS` until the handler settles. Self-chaining setTimeout
+     * (not setInterval) so each extend completes before the next is scheduled
+     * and the `finally` clearTimeout always cancels the pending tick. Extend
+     * failures log-and-continue — a single failed extend shouldn't kill the
+     * turn, and the lock will expire naturally if extends keep failing.
+     */
+    private dispatchWithHeartbeat;
     /**
      * Dispatch a message to the appropriate handler chain based on
      * subscription status, mention detection, and pattern matching.
diff --git a/dist/index.js b/dist/index.js
index 9a1901e407a776b9bfdfc05113295f1722b1ecde..ba5a03d6e854cd85142a560b0f0942bb30667377 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1904,6 +1904,7 @@ function reviver(_key, value) {
 
 // src/chat.ts
 var DEFAULT_LOCK_TTL_MS = 3e4;
+var LOCK_HEARTBEAT_MS = 15e3;
 function sleep(ms) {
   return new Promise((resolve) => setTimeout(resolve, ms));
 }
@@ -3158,7 +3159,7 @@ var Chat = class {
       await this.handleConcurrent(adapter, threadId, message);
       return;
     }
-    if (strategy === "queue" || strategy === "debounce") {
+    if (strategy === "queue" || strategy === "debounce" || strategy === "queue-debounce") {
       await this.handleQueueOrDebounce(
         adapter,
         threadId,
@@ -3207,7 +3208,7 @@ var Chat = class {
       token: lock.token
     });
     try {
-      await this.dispatchToHandlers(adapter, threadId, message);
+      await this.dispatchWithHeartbeat(lock, adapter, threadId, message);
     } finally {
       await this._stateAdapter.releaseLock(lock);
       this.logger.debug("Lock released", { threadId, lockKey });
@@ -3277,8 +3278,27 @@ var Chat = class {
           debounceMs
         });
         await this.debounceLoop(lock, adapter, threadId, lockKey);
+      } else if (strategy === "queue-debounce") {
+        await this._stateAdapter.enqueue(
+          lockKey,
+          {
+            message,
+            enqueuedAt: Date.now(),
+            expiresAt: Date.now() + queueEntryTtlMs
+          },
+          maxQueueSize
+        );
+        this.logger.info("message-queue-debouncing", {
+          threadId,
+          lockKey,
+          messageId: message.id,
+          debounceMs
+        });
+        await sleep(debounceMs);
+        await this._stateAdapter.extendLock(lock, DEFAULT_LOCK_TTL_MS);
+        await this.drainQueue(lock, adapter, threadId, lockKey);
       } else {
-        await this.dispatchToHandlers(adapter, threadId, message);
+        await this.dispatchWithHeartbeat(lock, adapter, threadId, message);
         await this.drainQueue(lock, adapter, threadId, lockKey);
       }
     } finally {
@@ -3322,7 +3342,7 @@ var Chat = class {
         lockKey,
         messageId: msg.id
       });
-      await this.dispatchToHandlers(adapter, threadId, msg);
+      await this.dispatchWithHeartbeat(lock, adapter, threadId, msg);
       break;
     }
   }
@@ -3369,7 +3389,46 @@ var Chat = class {
         skipped,
         totalSinceLastHandler: pending.length
       };
-      await this.dispatchToHandlers(adapter, threadId, latest.message, context);
+      await this.dispatchWithHeartbeat(
+        lock,
+        adapter,
+        threadId,
+        latest.message,
+        context
+      );
+    }
+  }
+  /**
+   * Run `dispatchToHandlers` with a heartbeat that extends `lock` every
+   * `LOCK_HEARTBEAT_MS` until the handler settles. Self-chaining setTimeout
+   * (not setInterval) so each extend completes before the next is scheduled
+   * and the `finally` clearTimeout always cancels the pending tick. Extend
+   * failures log-and-continue — a single failed extend shouldn't kill the
+   * turn, and the lock will expire naturally if extends keep failing.
+   */
+  async dispatchWithHeartbeat(lock, adapter, threadId, message, context) {
+    let timer;
+    let stopped = false;
+    const tick = () => {
+      timer = setTimeout(async () => {
+        if (stopped) return;
+        try {
+          await this._stateAdapter.extendLock(lock, DEFAULT_LOCK_TTL_MS);
+        } catch (err) {
+          this.logger.warn("Lock heartbeat extend failed", {
+            threadId,
+            error: err instanceof Error ? err.message : String(err)
+          });
+        }
+        if (!stopped) tick();
+      }, LOCK_HEARTBEAT_MS);
+    };
+    tick();
+    try {
+      await this.dispatchToHandlers(adapter, threadId, message, context);
+    } finally {
+      stopped = true;
+      if (timer) clearTimeout(timer);
     }
   }
   /**
